# pixi environments
.pixi/*
!.pixi/config.toml

# secrets
.env

# personal dev log, agent reference, and original planning doc — not for the repo
DECISIONS.md
agent.md
CLAUDE_ckd_guidelines_rag.md

# source PDFs (not committed — see README for how to obtain)
data/*.pdf

# vector store persistence
chroma_db/

# notebooks checkpoints / marimo runtime session cache
.ipynb_checkpoints/
notebooks/__marimo__/

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# local Claude Code session artifacts -- not project content
.claude/bash_history.log
.claude/scheduled_tasks.lock
.claude/settings.local.json
