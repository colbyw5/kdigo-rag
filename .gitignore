# pixi environments
.pixi/*
!.pixi/config.toml

# secrets
.env

# personal dev log and agent reference — not for the repo
DECISIONS.md
agent.md

# source PDFs (not committed — see README for how to obtain)
data/*.pdf

# vector store persistence
chroma_db/

# notebooks checkpoints
.ipynb_checkpoints/

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# local Claude Code session artifacts -- not project content
.claude/bash_history.log
.claude/scheduled_tasks.lock
.claude/settings.local.json
